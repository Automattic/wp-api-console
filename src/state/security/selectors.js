export const isReady = state => state.security.ready;

export const isLoggedin = state => state.security.isLoggedin;

export const getUser = state => state.security.user;

export const getToken = state => state.security.token;
